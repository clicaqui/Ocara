#envia arquivo
import paramiko 


	#paramiko.util.log_to_file("/home/"+self.user_logado+"/paramiko.log")
	host = "192.168.17.254" 
	port = 22 
	transport = paramiko.Transport((host, port))
	password = "liberdade" 
	username = "root" 
	transport.connect(username = username, password = password) 
	localpath = self.nome_arquivo 
	remotepath = "/home/tux/processos/"+self.nome_arquivo
	sftp = paramiko.SFTPClient.from_transport(transport)  
	sftp.put(localpath, remotepath) 
	sftp.close() 
	transport.close() 

